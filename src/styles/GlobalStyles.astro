<style lang="scss" is:global>
	@import '/fonts/Gelion/stylesheet.css';
	@import 'GlobalVariables.scss';
	html {
		font-family: 'Gelion', sans-serif;
		scroll-behavior: smooth;
	}
	a {
		text-decoration: none;
		user-select: none;
	}

	svg {
		outline: none;
	}

	* {
		box-sizing: border-box;
	}
	body {
		margin: 0 auto;
		overflow-x: hidden;
		scroll-behavior: smooth;
	}

	#main-container {
		width: 100%;
		transition: width 0.6s ease-in-out;
	}

	.pushed {
		transition: width 0.6s ease-in-out;
		width: calc(100% - #{$navbar_menu_width} + #{$scrollbar_width_px}) !important;
	}
</style>
